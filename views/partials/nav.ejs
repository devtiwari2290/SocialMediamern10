<nav class="flex justify-evenly gap-x-10 py-7 bg-black">
    <div class="text-2xl font-extrabold text-white">Logo</div>
    <div class="flex gap-x-10">
       <% if(user){ %>
        <a class="text-white hover:text-red-400" href="/user/profile">Home</a>

       <% } else{ %>
        <a class="text-white hover:text-red-400" href="/">Home</a>

      <% } %>
      <a class="text-white hover:text-red-400" href="/about">About</a>
      <a class="text-white hover:text-red-400" href="/contact">Contact</a>
    </div>


    <div>

        <% if(user){ %>
            <a class="text-white hover:text-red-400" href="/user/logout">Login</a>

            <% }else { %>
        
                <a class="text-white hover:text-red-400" href="/login">Login</a>

                <% } %>
    </div>
   
</nav> 